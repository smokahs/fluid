# gradle
org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false

# minecraft and forge
minecraft_version=1.20.1
minecraft_range=[1.20.1,1.20.2)
forge_version=47.4.0
forge_range=[47,)
parchment_version=2023.09.03

# tooling
mixin_version=0.8.5

# mod metadata. nothing here is compiled against another mod on purpose: the only mixin target is
# vanilla, and every other mod is reached through json and the fluid registry.
mod_id=fluidify
mod_name=Fluidify
mod_version=1.0.0
mod_group=com.fluidify
mod_license=MIT
